========================================================================
 PAYME INTEGRATSIYASI — HOLAT VA KEYINGI QADAMLAR
 Loyiha: TaxiNarx (C:\Users\user\Desktop\Muhammad\taxsi_narxlar)
 Yangilangan sana: 2026-07-18
 Bu faylni Claude'ga ko'rsatsangiz, u qayerda qolganimizni bilib,
 KEYINGI QADAM (frontend moslash) dan davom ettiradi.
 Eski batafsil hisobot: popka_fayl_pyme\PAYME_INTEGRATSIYA_HISOBOT.txt
========================================================================

------------------------------------------------------------------------
1. BUGUNGACHA QILINGANLAR (tayyor, qayta qilish SHART EMAS)
------------------------------------------------------------------------
[x] Webhook kodi (backend\apps\billing\payme.py) — 6 JSON-RPC metod,
    lokal sandbox-simulyatsiyada 24/24 test o'tgan (2026-07-14).
[x] 2026-07-18: merchant.payme.uz kabinetida KASSA YARATILDI:
      - Sfera: Транспорт -> Такси, brend: TaxiScan
      - Turi: "Прием электронных платежей с биллингом"
      - Endpoint URL: https://backend.taxiscan.app/api/v1/billing/payme/
      - Rekvizit: order_id (faqat raqamlar, ^[0-9]+$)
[x] Merchant ID olindi: 6a5ba195292f4a26eb200efe
[x] TEST kaliti olindi va backend\.env ga yozildi (PAYME_TEST_KEY).
    DIQQAT: kalitning o'zi FAQAT backend\.env da — bu txt'ga atayin
    yozilmadi (git'ga tushib qolmasligi uchun).
[x] Lokal tekshiruv O'TDI (Django shell + test client):
      - .env dan merchant id + test kalit to'g'ri o'qilyapti
      - checkout_url() to'g'ri havola yasayapti (m=<merchant_id>;
        ac.order_id=..;a=<tiyin>;l=uz, base64)
      - webhook: to'g'ri test kalit -> auth o'tadi; noto'g'ri -> -32504
[ ] PROD kaliti (PAYME_KEY) hali YO'Q — kassa jonli rejimga o'tgach
    "Инструменты разработчика" bo'limidan olinadi.

------------------------------------------------------------------------
2. KEYINGI QADAM — CLAUDE SHU YERDAN BOSHLAYDI
------------------------------------------------------------------------
[x] 1. WEB FRONTEND obuna sahifasi yangi Payme checkout oqimiga MOSLANDI
       (2026-08-09, Claude). Bajarilgan:
         - frontend\src\app\(app)\billing\page.tsx to'liq qayta yozildi:
             subscribe() -> POST /billing/payme/checkout/ -> checkout_url
             ni window.open bilan yangi oynada ochadi -> "To'lov kutilmoqda"
             modali GET /billing/payme/status/?order_id=N ni 3s da polling
             qiladi -> paid=true bo'lgach obunani yangilaydi.
         - Narx endi rolga qarab so'mda ko'rsatiladi (monthly_price_uzs);
           eski "$1/oy ~12,500 so'm" olib tashlandi.
         - ESKI SOXTA KARTA OQIMI olib tashlandi: /billing dagi "Kartalarim"
           + AddCardForm, passenger/driver profildagi "Kartalarim" bo'limlari
           o'chirildi (Payme'da karta o'z sahifasida kiritiladi, saqlanmaydi).
         - Landing (app/page.tsx): "Faqat 1$/oy" -> "Oyiga 9 999 so'mdan".
         - types.ts ga PaymeCheckout + Subscription.monthly_price_uzs/
           discount_percent qo'shildi. `npx tsc --noEmit` -> 0 xato.
       ESLATMA: mobil (Expo/RN) BillingScreen'da WebView + polling
       ALLAQACHON to'liq ishlaydi — tegilmadi.
       Lokal e2e test O'TDI (2026-08-09): checkout -> CreateTransaction ->
       PerformTransaction -> status paid=true -> obuna +30 kun (backend
       Django test client + sandbox kalit simulyatsiyasi, 6/6 metod OK).

------------------------------------------------------------------------
3. UNDAN KEYINGI QADAMLAR (tartib bilan)
------------------------------------------------------------------------
[ ] 2. Serverga yuklash (server 89.249.60.118, ~/logfree/taxiscan):
         - git push (lokal) -> serverda git pull
         - serverdagi backend/.env ga PAYME_MERCHANT_ID va
           PAYME_TEST_KEY ni qo'shish (lokal .env dan olinadi)
         - python manage.py migrate (0006 va 0007 SHART)
         - pm2 restart backend
       Eslatma: Claude'da server SSH kaliti YO'Q — buyruqlarni user
       o'zi bajaradi, Claude tayyorlab beradi.
[ ] 3. User 443 portni ochadi (Kerio Control + PFX rejasi) —
       https://backend.taxiscan.app/api/v1/billing/payme/ tashqaridan
       ochilishi SHART (Payme shunga so'rov yuboradi).
[ ] 4. test.paycom.uz sandbox'da RASMIY test: endpoint URL + merchant id
       + test kalit kiritiladi, barcha stsenariylar o'tkaziladi
       (lokalda 24/24 o'tgan — muammo kutilmaydi).
[ ] 5. Kabinetda kassani JONLI rejimga o'tkazish (moderatsiya),
       PROD kalitni olib backend\.env dagi PAYME_KEY= ga yozish
       (lokal ham, server ham), pm2 restart, haqiqiy to'lov sinovi.

------------------------------------------------------------------------
FOYDALI MA'LUMOTLAR
------------------------------------------------------------------------
- Kabinet: https://merchant.payme.uz (kassa -> "Настройки" = Merchant ID,
  "Инструменты разработчика" = TEST/PROD kalitlar)
- Sandbox: https://test.paycom.uz
- Protokol hujjati: https://developer.help.paycom.uz/protokol-merchant-api/
- Endpointlar (prefiks /api/v1/billing/):
    POST payme/           <- Payme webhook (JSON-RPC)
    POST payme/checkout/  <- checkout havola olish (auth kerak)
    GET  payme/status/    <- to'lov holati polling (auth kerak)
- Narxlar: yo'lovchi 9 999 so'm/oy, haydovchi 49 999 so'm/oy (tiyinda x100)
- Lokal test user: +998901112233 / test12345 (db.sqlite3)
========================================================================
